docker image - It is a lightweight, standalone, executable package that contains everything needed to run a piece of software, including:
Is docker image executable ?
----------------------------

what is this line doing

RUN addgroup -S appgroup && adduser -S appuser -G appgroup

USER appuser

## copy from drive

## shouldn't the prod ready dockerfile have ports exposed, network and volumes ? or are they used in docker compose only ? Can i see whats happening behind the scene in docker, while doing docker run

do we need docker compose if using k8 ?

---

i want to see java logs like that of maven, run and cmd to undestand the command that i would be passing in dockerfile
